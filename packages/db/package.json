{
  "name": "@jarvis/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "migrate": "tsx src/migrate.ts",
    "import:sqlite": "tsx src/import-sqlite.ts"
  },
  "dependencies": {
    "@jarvis/shared": "workspace:*",
    "mongodb": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
